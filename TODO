- align total counter to the small values
- insert graph and axis labels
- average since last reset, since last start, since beginning
- open db rw (and create blank db if not existent) in db_client.c
- make db_client use sqlite.c as well

- implement websockets-push, or play with poll time
- create HTML on the fly
